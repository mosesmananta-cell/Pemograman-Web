let data = "true";
console.log(data, "-> Tipenya:", typeof data);

// Mengubah tipe data ke angka (number)
data = 400;
console.log(data, "-> Tipenya:", typeof data);

// Mengubah tipe data ke boolean (true/false)
data = false;
console.log(data, "-> Tipenya:", typeof data);